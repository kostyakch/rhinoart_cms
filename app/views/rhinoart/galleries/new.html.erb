<% provide(:title, t(:creating_gallery)) %>

<% render 'sidebar' %>

<%= form_for @gallery do |f| %>
    <legend><%= t(:creating_gallery) %></legend>   

   	<%= render 'fields', f: f %>	   
<% end %>